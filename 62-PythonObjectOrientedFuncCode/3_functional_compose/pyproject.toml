[project]
name = "ai_puzzle"
version = "0.1.0"
description = "Cool AI tool"
authors = [{name = "ArjanCodes"}]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.7.2",
    "python-dotenv>=1.0.0",
    "pydantic>=2.6.4",
]

[dependency-groups]
dev = [
    "pytest>=7.4.4",
]
