# Specification Pattern

1. Encapsulate Business rules into specifications, Combine specifications using business logic 
2. Each class can be a rule specification

Rules usable and composable - use functional framework with decorators and generators
Predicate - fn that returns true / false

1. Specify logic or specifications first combined
2. Later on, execute

Predicate allows to specify conditions without executing them.

g 
