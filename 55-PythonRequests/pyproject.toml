[project]
name = "PythonRequests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests",
    "python-dotenv"
]

[tool.pytest.ini_options]
pythonpath = "."