[project]
name = "ds-after"
version = "0.1.0"
description = "Data Science project - after refactoring"
requires-python = ">=3.9"
dependencies = [
    "torch",
    "numpy",
    "pandas",
    "scikit-learn",
    "matplotlib",
    "tensorboard",
    "mlflow",
    "tqdm",
    "pillow",
]
