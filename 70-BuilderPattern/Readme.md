# Builder Pattern

Build complex objects step by step in clean way
Seperate object construction from representation


Example build html page

When use Builder pattern 
Complex object with optional parts
Config file, nested queries
Clean fluent api 

seperate construction from logic.
Avoid giant constructors

Adds boiler plate  and easy to make mistake ex. forget  build method